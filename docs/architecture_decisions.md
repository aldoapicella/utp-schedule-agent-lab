# Architecture Decisions

- Keep scheduling deterministic and reusable.
- Use local JSON plus SQLite instead of live UTP integrations.
- Prefer typed tools and explicit validation over free-form agent execution.
- Expose traces and human review state through the API for the dashboard.

